Create this exact architecture diagram in French.

Title:
Architecture Microservices ISIMM

Nodes and labels (keep this wording):
1) Utilisateurs
	subtitle: Candidat, Commission, Administrateur

2) Angular Client (frontend web)
	subtitle: Frontend web application.

3) Auth Service (Django, JWT, roles, login)
	subtitle: Handles authentication, JWT generation, roles, and login.

4) Auth DB (PostgreSQL)
	subtitle: Stores accounts and tokens.

5) User Service (Django gestion profils utilisateurs)
	subtitle: Manages user profiles.

6) User DB (PostgreSQL)
	subtitle: Stores user profiles.

7) Candidature Service (Django, dossiers, status, reclamations, notifications)
	subtitle: Manages applications, statuses, claims, and notifications.

8) Candidature DB (SQLite)
	subtitle: Stores applications, documents, notifications.

9) Redis Broker + Celery Worker/Beat
	subtitle: Connects to Candidature Service for asynchronous tasks.

10) File Storage local (media)
	 subtitle: Local file storage linked to Candidature Service.

11) Taches asynchrones
	 subtitle: Notifications, rappels, traitement OCR.

Main connections:
- Utilisateurs -> Angular Client
- Angular Client -> Auth Service
- Auth Service -> Auth DB
- Auth Service -> User Service
- User Service -> User DB
- User Service -> Candidature Service
- Candidature Service -> Candidature DB
- Candidature Service -> Redis Broker + Celery Worker/Beat
- Candidature Service -> File Storage local (media)
- Redis Broker + Celery Worker/Beat -> Taches asynchrones

Additional service-to-service/API relations (as in the reference image):
- Angular Client -> User Service
- Angular Client -> Candidature Service
- Auth DB <-> User Service
- Auth Service -> Candidature Service

Layout constraints (important):
- Place nodes vertically and horizontally exactly like the reference:
  Utilisateurs top center, Angular below, Auth left, User center, Candidature right,
  DBs below their services, Redis/File Storage below Candidature, Taches asynchrones at bottom.
- Use orthogonal lines only (90 degrees).
- No diagonal links.
- Keep the same blue cards and green DB cylinders.
- Black background.

Critical arrow rule:
- Do NOT attach arrowheads to the top edge of any rectangular service box.
- Incoming arrows to rectangular boxes must enter from left, right, or bottom edges only.
- Keep arrowheads clean and outside labels.

Visual style:
- Modern clean architecture infographic.
- Preserve spacing so links do not cross node text.
